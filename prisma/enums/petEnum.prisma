// 애완동물 타입
enum PetType {
    DOG  @map("강아지")
    CAT  @map("고양이")
    BIRD @map("새")
    FISH @map("물고기")
}

// 활동 타입
enum PetActivityType {
    WALK        @map("산책")
    MEAL        @map("식사")
    SNACK       @map("간식")
    HOSPITAL    @map("병원")
    GROOMING    @map("미용")
    PLAY        @map("놀이")
    TRAINING    @map("훈련")
    MEDICINE    @map("약")
    BATH        @map("목욕")
    VACCINATION @map("예방접종")
    WEIGHT      @map("체중측정")
    ETC         @map("기타")
}

// 활동 상태
enum PetActivityStatus {
    SCHEDULED @map("예정")
    COMPLETED @map("완료")
    CANCELLED @map("취소")
    MISSED    @map("놓침")
}
